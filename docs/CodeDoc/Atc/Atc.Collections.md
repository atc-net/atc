<div style='text-align: right'>

[References](Index.md)&nbsp;&nbsp;-&nbsp;&nbsp;[References extended](IndexExtended.md)
</div>

# Atc.Collections

<br />

## ConcurrentHashSet&lt;T&gt;
Provides a thread-safe hash set implementation using reader-writer locks. This collection allows multiple concurrent readers or a single writer at a time.

>```csharp
>public class ConcurrentHashSet&lt;T&gt; : IEnumerable<T>, IEnumerable, IDisposable
>```

### Properties

#### Count
>```csharp
>Count
>```
><b>Summary:</b> Gets the number of elements contained in the hash set. This operation acquires a read lock.
### Methods

#### Clear
>```csharp
>void Clear()
>```
><b>Summary:</b> Removes all elements from the hash set in a thread-safe manner. This operation acquires a write lock.
#### Contains
>```csharp
>bool Contains(T item)
>```
><b>Summary:</b> Determines whether the hash set contains the specified element. This operation acquires a read lock.
>
><b>Parameters:</b><br>
>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;`item`&nbsp;&nbsp;-&nbsp;&nbsp;The element to locate in the hash set.<br />
>
><b>Returns:</b> `true` if the hash set contains the specified element; otherwise, `false`.
#### Dispose
>```csharp
>void Dispose()
>```
#### FirstOrDefault
>```csharp
>T FirstOrDefault(Func<T, bool> predicate)
>```
><b>Summary:</b> Returns the first element in the hash set that satisfies the specified condition, or a default value if no such element is found. This operation acquires a read lock.
>
><b>Parameters:</b><br>
>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;`predicate`&nbsp;&nbsp;-&nbsp;&nbsp;A function to test each element for a condition.<br />
>
><b>Returns:</b> The first element that satisfies the condition, or the default value for type `T` if no such element is found.
#### GetEnumerator
>```csharp
>IEnumerator<T> GetEnumerator()
>```
#### TryAdd
>```csharp
>bool TryAdd(T item)
>```
><b>Summary:</b> Attempts to add an element to the hash set in a thread-safe manner. This operation acquires a write lock.
>
><b>Parameters:</b><br>
>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;`item`&nbsp;&nbsp;-&nbsp;&nbsp;The element to add to the hash set.<br />
>
><b>Returns:</b> `true` if the element was added successfully; `false` if the element already exists.
#### TryRemove
>```csharp
>bool TryRemove(T item)
>```
><b>Summary:</b> Attempts to remove an element from the hash set in a thread-safe manner. This operation acquires a write lock.
>
><b>Parameters:</b><br>
>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;`item`&nbsp;&nbsp;-&nbsp;&nbsp;The element to remove from the hash set.<br />
>
><b>Returns:</b> `true` if the element was removed successfully; `false` if the element was not found.
<hr /><div style='text-align: right'><i>Generated by MarkdownCodeDoc version 1.2</i></div>
